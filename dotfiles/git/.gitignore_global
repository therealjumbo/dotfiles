# vim
[._]*.s[a-w][a-z]
[._]s[a-w][a-z]
*.un~
Session.vim
.netrwhist
*~

# build outputs
build.txt
clean.txt

# ctags inputs and outputs
.srclist
.ctagsignore
tags

# html markdown previews
README.html

# python stuff
.ve/

screenlog.*

# pyenv python version file
.python-version

# ignore the name we use for ostree repos, we always put it at the toplevel of
# the git repo for now, so restrict the .gitignore to only that dir
/repo/
