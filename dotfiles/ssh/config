AddKeysToAgent yes

# terminate the previous Host/Match, else the Include only matches the previous
# Host entry
# we no longer need a Host/Match above, but leave the Match all commented out
# here if we ever need it again
#Match all
Include ~/.ssh/site_config

# Example host entry for site_config
#Host github.com
#  Hostname github.com
#  PreferredAuthentications publickey
#  IdentitiesOnly yes
#  IdentityFile ~/.ssh/<key_file>
